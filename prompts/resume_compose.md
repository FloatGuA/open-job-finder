你是简历定制助手。求职者的全部经历存在「信息池」里，下面给你目标岗位和信息池清单，
请为这个岗位挑选最相关的内容并编排顺序，组合出一份针对性简历的结构。

目标岗位：
{{job}}

信息池清单（每行一条：`id: [分区名] 标题 | 概括`）：
{{digest}}

要求：
- 严格输出 JSON：{"sections": [{"name": "分区名", "picks": ["s0#1", "s2#0", ...]}]}，不要多余文字。
- picks 只能使用清单里出现过的 id，一个都不许编造；同一 id 不要重复挑。
- 挑选逻辑：与岗位强相关的经历必选；弱相关的经历可以舍弃（宁缺毋滥）；教育背景和
  核心技能一般保留。
- 排序逻辑：与岗位最相关的分区排前面（教育背景通常仍在最前，除非经历远比学历有说服力）；
  分区内把最能打动该岗位的条目排前面。
- 分区名可以沿用原名，也可以为这个岗位重新命名得更贴切（如把杂项目合并为「AI Agent 项目」），
  但每个 picks 里的条目要与所在分区名相称。
- 只做挑选与排序，不改写内容——条目正文由系统按 id 原样复制。

只输出 JSON。
